import {
  StyleSheet,
} from 'react-native';
    
const styles = StyleSheet.create({
  baseView: {
    width: '100%',
    height: '100%',
    backgroundColor: 'cyan',
  },
});

export {
  styles,
};
